# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.

## Prototype-specific direction

- Treat the supplied MP4 recording as the primary visual and interaction source: black dot-grid workspace, fluorescent paper colors, tactile document/folder metaphors, bold condensed type, experimental 3D objects, and energetic scroll/hover motion.
- Treat https://www.wildyriftian.com/works as a secondary reference only, mainly for large editorial headings, generous whitespace, and the scannable archive structure.
- Match the reference typography system with JetBrains Mono 400 for navigation, first-screen helper text, English UI labels, and numbers; keep Georgia for editorial archive/folder titles and DM Sans for Chinese body copy.
- The homepage keychain contains seven full-canvas positioned PNG layers in this durable order: 01 ABOUT ME, 02 DOUYIN, 03 MEITUAN, 04 KUAISHOU, 05 BRAND, 06 EXERCISES, 07 AI LAB. Preserve each supplied PNG's baked-in placement and extend the existing ring-pivot hover fan without translating individual pendants.
- The Works page uses a warm `#EACEA7` canvas, `#937549` Works heading, a lighter warm-brown Archive heading, and folder colors 01 `#66DEFF`, 02 `#FFED7A`, 03 `#AFFF62`, 04 `#FEB8E0`, 05 `#DCDCDC`, 06 `#323232`.
- Works navigation is two-level: each folder first opens a project collection, then a project opens its detail view with a back action. DOUYIN, MEITUAN, and KUAISHOU use structured case-study grids; BRAND, Exercises, and AI LAB use a looser visual-stream grid. Until real project content arrives, keep placeholder copy explicit rather than inventing achievements.
- Work collections and project details are full-screen route-like states, not centered modals: no dimmed overlay and no `CLOSE` button. Preserve the reference site's four-part top navigation, left-side animated back action, pendant/collection split layout, and editorial project detail split.
- AI LAB's project collection uses the same title scale, row height, image slot, and typography spacing as the other work collections; only its supplied keychain/preview artwork receives a scoped optical position correction for its baked-in crop.
- Collection project rows expand gently on hover from roughly 150px to 285px, revealing year/role metadata and a larger preview.
- DOUYIN project `01.01` uses the real 2026 Spring Festival assets in the curated display order `01` through `13`, the two split-horse screens, the original `15` and `14` assets, then the latest added final screen; do not use lexicographic filename order or replace these with generated placeholders.
- CONTACT is a full-height page directly below WORKS in the homepage waterfall, not a modal overlay and not the earlier plain-blue `LET’S CREATE SOMETHING USEFUL.` section. Its black workspace, layered blue folder and clipped papers, pink note, supplied WeChat QR code, and draggable hanging work-permit badge follow the supplied recording. The top navigation scrolls to this page; project-collection CONTACT navigation exits the collection and lands here. Pulling the badge retracts only the badge upward and keeps the viewer on the CONTACT page. Do not include the reference's neon-green close button. Keep the live contact details as WeChat `agenzZ`, phone `18500636059`, and email `agenzzk@163.com`.
- CONTACT's left eyebrow copy is `欢迎与我联系>_`. Its front folder field uses the reference blue `#67C4EC`, the rear folder and left tab use `#44AAD8`, and the sticky note uses `#ffb8e0`.
- CONTACT's two blue folder layers now follow the supplied macOS folder-icon reference: a compact raised rear tab at the far left, clean horizontal front edge, exact flat cyan fills, and small approximately 10px corners rather than large pill-like radii. The hanging work-permit card is narrow, tall, and rounded. Keep box shadows off both the permit card and pink note. Text on the blue folder surfaces uses a high-contrast deep navy (`#082c45`) with muted navy secondary labels rather than white.
- CONTACT replaces the earlier white and fluorescent-green inserts with six straight-corner file cards using the WORKS folder colors in order: 01 `#66DEFF`, 02 `#FFED7A`, 03 `#AFFF62`, 04 `#FEB8E0`, 05 `#DCDCDC`, and 06 `#323232`. Keep all six horizontal with no rotation, like documents stacked inside a macOS folder icon.
- CONTACT's blue folder follows the compact macOS folder-icon anatomy: a cyan rear panel with a raised tab, a smooth cyan front panel, and the six horizontal files between them. Hovering the blue front panel gently lowers it and raises the cards in a sequential 01–06 stack; hovering a raised card keeps the folder open. Each file card is a real button that opens its corresponding WORKS collection.
- CONTACT's front blue panel includes a dedicated narrow Gaussian-blurred shadow along its upper edge. Keep it soft and translucent so it creates depth across the inserted files without reading as a gray strip or hard line.
- CONTACT's draggable work permit is the single supplied raster asset `/assets/contact-work-badge.png`; it already includes the pin, card, typography, QR code, and rounded silhouette. Do not rebuild or overlay any of those parts in HTML/CSS. Clicking the email copies it and must not use `mailto:` or launch a mail application.
